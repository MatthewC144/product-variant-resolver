# AI artifact rubric — T49.3 HSP-2 adapter

2026-09-14, Lite. PASS for code/private mocks/source freeze; SQL/runtime/cost gates NOT RUN.

Grounding PASS:exact142/100/42snapshot,12source plan, v4 artifact/protocol and42 accepted runtime
inputs; no guessed color/wheel/tampo/release facts. Authority PASS:canonical flow delegates unchanged,
human is debug-only, original entrypoint/source/default untouched and nondebug samples byte-equal.
Integrity PASS in mocks:strict/no-duplicate profile, committed source equality, whole-source probe,
sticky503/no fallback, invalidHTTP preservation and accepted candidate2 manifest. Auditability PASS:
candidate1 retained/superseded, failed test assumptions/source-pin gap recorded, runtime flags null/false.
Safety LIMITED PASS:URL external/not logged, contained source paths and disposable DB-name pattern;
actual SELECT-role permissions/network/containers are untested. Cost NOT RUN:debug timing field exists
but no latency number is scored. Extendability BOUNDED:separate modes and wrapper avoid old source edits;
full reads at3k may be expensive and require measurement before redesign.

Evidence:55focused/544full tests PASS, Ruff/MyPy/compileall/checkers PASS. This rubric does not turn fake
repository calls into PostgreSQL evidence or four sample canonical comparisons into exact199 parity.
HSP3–4/fullT49.3/T49.4 remain pending; new isolated SQL run still needs explicit owner approval.
