# AI artifact rubric — T49.2 human snapshot SQL storage

Date:2026-09-14. Lite. Scoped SQL correctness artifact PASS; no RAG accuracy/production rollout claim.

Grounding PASS:real PostgreSQL stores/readbacks exact142pinned typed documents and raw provenance;
original UUIDs/types/source contracts unchanged. No invented release/color/wheel/tampo facts.
Authority PASS:separate explicitly approved disposable human namespace, no canonical admission or
defaultAPI/v4 change; seven canonical tables retain identical row/UUID/timestamp snapshots.
Integrity PASS:genuine unique violation after71visible docs rolls back header/all children; concurrent
first imports yield one insert/one verified no-op; repeat imported_at unchanged. Invalid source-bound
inputs and deliberately corrupt/partial stored data fail closed without repair.
Safety PASS:new internal network/tmpfs/nohostports/read-only staged runner; literal authorization+
matching explicit DB/server guard; no production URL fallback, no oldvolume/container mutations.
Two newcontainers/network cleaned by exactownership; remaining0/errors0. Process-kill cleanup and
privileged SQL writer-proof immutability are not claimed. HistoricalT04revision-bound harness is not
silently declared compatible with0002; new isolated verifier covers the new migration cycle.
Auditability PASS:actual stdout/stderr, image IDs,54source fingerprints, plan/canonical checksums,
scoped owner consent, narrative log and SQL gates.25new/473full tests PASS; stored report checker PASS.
Cost/extendability SCOPE ONLY:bounded142doc SQL correctness, no per-query latency/real3k/ANN claim;
serial table locks and optional dependencies are intentional. New runtime profile/protocol required
before future integration; existing final105outputs preserved and no new final collector invoked.

See `docs/evidence/t49-2-human-knowledge-postgres.md` and the raw report for actual evidence.
